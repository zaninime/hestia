[package]
name = "hestia"
version = "3.0.2-beta.1"
edition = "2024"
license = "MIT"
description = "Nix binary cache backed by the GitHub Actions cache (v2 API)"
repository = "https://github.com/Mic92/hestia"
keywords = ["nix", "binary-cache", "github-actions", "cache", "ci"]
categories = [
  "caching",
  "development-tools::build-utils",
  "command-line-utilities",
]

[workspace]
members = ["hestia-core"]

[dependencies]
hestia-core = { path = "hestia-core" }

# CLI / runtime
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = [
  "rt-multi-thread",
  "macros",
  "net",
  "signal",
  "time",
  "sync",
  "io-util",
  "process",
] }

# Accept-loop errno classification (unix sockets are unix-only anyway)
libc = "0.2"

# Serialization
serde = { version = "1", features = ["derive"] }
zstd = "0.13"

# HTTP client (Twirp, GitHub REST, Azure blob) and substituter server
reqwest = { version = "0.13", default-features = false, features = [
  "rustls",
  "json",
  "query",
] }
axum = "0.8"

# Internal content addresses (chunk + pack hashes); faster than SHA-256
blake3 = "1"

# Stream combinators for NAR event/byte streams
futures-util = "0.3"

# Errors
thiserror = "2"

# Blob payloads (cheap clones for upload retries)
bytes = "1"

# JSON bodies for the Twirp / REST / Azure APIs
serde_json = "1"

# Harmonia crates (not on crates.io; pinned to a specific rev)
# Harmonia crates intentionally not used (re-add when needed):
#   harmonia-store-remote        -> path info comes from direct store
#                                    database reads (harmonia-store-db),
#                                    which works without a running daemon
#   harmonia-utils-base-encoding -> nothing needs raw base32 yet
harmonia-store-nar-info = { git = "https://github.com/nix-community/harmonia", rev = "bba51756f92a488d440e4c426747fb681abce4a7" }
harmonia-store-path = { git = "https://github.com/nix-community/harmonia", rev = "bba51756f92a488d440e4c426747fb681abce4a7" }
harmonia-store-path-info = { git = "https://github.com/nix-community/harmonia", rev = "bba51756f92a488d440e4c426747fb681abce4a7" }
harmonia-utils-io = { git = "https://github.com/nix-community/harmonia", rev = "bba51756f92a488d440e4c426747fb681abce4a7" }
harmonia-utils-signature = { git = "https://github.com/nix-community/harmonia", rev = "bba51756f92a488d440e4c426747fb681abce4a7" }

[dev-dependencies]
# Integration tests spawn the hestia binary and nix tooling
tokio = { version = "1", features = ["process"] }
# Property-based tests for manifest merge laws
proptest = "1"
# Blob storage directory for the fake GHA backend
tempfile = "3"
