//! Cache v1 (`_apis/artifactcache`) client for Gitea / Forgejo.

// The v1 client is implemented in a follow-up change (the `V1` arm of
// `crate::gha::client::CacheClient`): this module exists so that `mod.rs`
// can declare `pub mod v1;` already.
